// Legacy entry point, kept for backward compatibility.
// The published package resolves to src/parser.mjs (ESM) / src/parser.cjs (CJS) instead.
export { default, parseSouthAfricanId } from "./src/parser.mjs";
